import React from "react";

/**
 * UI for Website's navigation bar.
 * Note for self, review previous projects for how I developed this.
 * 
 * @returns JSX Element
 */
const NavBar = (): JSX.Element => {
    return (
        <div>

        </div>
    );
};

export default NavBar;