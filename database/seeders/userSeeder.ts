/* SEEDERS: userSeeder.ts */

// Acts as a template for future seeders.
export const users = {
    columns: '(image,username,password,email)',
    rows: [
        ["ex1", "ex1", "ex1", "ex1"], 
        ["ex2", "ex2", "ex2", "ex2"]
    ]
}