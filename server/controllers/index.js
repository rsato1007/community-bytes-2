// Controllers index.js

/*
    This file acts as a "hub" for all controller files. Seperating the routes,
    middleware, and controllers into seperate files isn't necessary, but
    results in better organization I believe.
*/

module.exports = {
    // The examples below are for your reference.
    // users: require("./users"),

    // Each file below imports controller functions needed
    // Authentication Controller
    // Users Controller
    // Recipes Controller
    // Ingredients Controller
    // Reviews Controller
};